[package]
name = "fix-test"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version="0.12", default-features = false, features = ["blocking", "json", "rustls-tls"]}
serde_json = "1"
rand = "0.9"
chrono = "0.4"